<div class="col-3">
    <div class="list-group">
        {% if app.request.attributes.get('_route') == 'profile' %}
            <a type="button" href="{{ path('profile') }}" class="list-group-item list-group-item-action active">
                Mon profil
            </a>
        {% else %}
            <a type="button" href="{{ path('profile') }}" class="list-group-item list-group-item-action">
                Mon profil
            </a>
        {% endif %}

        {% if app.request.attributes.get('_route') == 'profile_achievements' %}
            <a type="button" href="{{ path('profile_achievements') }}" class="list-group-item list-group-item-action active">
                Mon avancement
            </a>
        {% else %}
            <a type="button" href="{{ path('profile_achievements') }}" class="list-group-item list-group-item-action">
                Mon avancement
            </a>
        {% endif %}

        {% if app.request.attributes.get('_route') == 'profile_actions' %}
            <a type="button" href="{{ path('profile_actions') }}" class="list-group-item list-group-item-action active">
                Actions
            </a>
        {% else %}
            <a type="button" href="{{ path('profile_actions') }}" class="list-group-item list-group-item-action">
                Actions
            </a>
        {% endif %}
    </div>
</div>